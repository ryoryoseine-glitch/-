# ビデオシューティングアプリ

ブラウザの `MediaDevices` / `MediaRecorder` API を使ったシンプルなビデオ撮影アプリです。

## 使い方

1. 任意の静的サーバーでこのディレクトリを配信します。
2. ブラウザで開き、「カメラ開始」を押します。
3. 「録画開始」→「録画停止」で撮影します。
4. 「動画を保存」で録画ファイル（`.webm`）を保存できます。

## ローカル起動例

```bash
python3 -m http.server 8000
```

`http://localhost:8000` にアクセスしてください。
